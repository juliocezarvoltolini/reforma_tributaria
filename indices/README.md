# /indices/ — derivados

Tudo aqui é gerado por `pipeline/indexar.py` a partir de `/normas/`.

**Nunca editar à mão.** Regenere:

    python3 pipeline/indexar.py

`cobertura.json` diz o que está efetivamente ingerido — consulte antes de
presumir que uma norma está na base.
